# Conan Exiles Admin Map

[English](README.md) | **Русский**

Админ-панель для серверов Conan Exiles — просмотр игроков, строений и рабов на интерактивной карте.

## Возможности

- Интерактивная карта с поддержкой обеих карт: **Изгнанники (Exiled Lands)** и **Остров Сиптах (Isle of Siptah)**
- Метки для всех основных типов объектов:
  - Игроки (онлайн выделяются)
  - Питомцы
  - Рабы
  - Постройки (фундаменты)
  - Ремесленные станки
  - Алтари
  - Троны
  - Загоны для животных
  - Спальники / Кровати
  - Костры / Большие костры
  - Сундуки
  - Картографические комнаты
  - Требушеты
  - Хранилища
  - Колодцы
  - Колёса боли
  - Рыболовные ловушки / Ловушки для моллюсков
  - Все объекты Pippi и актёры (Thespians)
- **Мультисерверность** — управление несколькими серверами Conan Exiles из одной панели
- **Кэширование снимков** — данные загружаются по запросу (кнопка «Обновить»); сервер никогда не читает `game.db` автоматически
- **Доступ по пользователям** — каждый аккаунт можно ограничить конкретными серверами
- **Глобальный поиск** (Ctrl+F) — найти любую метку по имени
- **Список строений / кланов** — гильдии с числом построек и индикаторами активности
- **Тултипы меток** с бейджами тира/альфы и командой `TeleportPlayer` в один клик
- Фильтрация меток по кланам (множественный выбор)
- Переключение между картами Изгнанников и Острова Сиптах
- Тёмный интерфейс
- Быстрая загрузка тайлов (WebP) и долгое кэширование в браузере
- Защита паролем через файл конфигурации (Basic Auth)
- **Иконка в системном трее** — остановить сервер без диспетчера задач
- **Автозапуск браузера** при старте

## Установка

1. Скачайте последний `.zip` со страницы [Releases](https://github.com/Myp3uK/ConanMap/releases).
2. Распакуйте в любое удобное место (рядом с `game.db` находиться **не обязательно**).
3. Отредактируйте `conan-exiles-admin-map.ini` — укажите пути к базам, порт, язык и при необходимости учётные данные.
4. Запустите `conan-exiles-admin-map.exe` — браузер откроется автоматически, появится иконка в трее.
5. Нажмите кнопку **Серверы** (🖥) в боковой панели, затем **Обновить**, чтобы загрузить данные из базы.
6. Для остановки используйте иконку в трее → **Stop server** (или закройте через диспетчер задач).

### Конфигурация (`conan-exiles-admin-map.ini`)

#### Один сервер (минимально)

```ini
[SETTINGS]
language = ru       ; ru, en или es
port     = 3001

[SERVER_server1]
name             = Мой сервер
database         = C:/ConanExiles/Saved/game.db
refresh_cooldown = 300              ; секунд между ручными обновлениями
```

#### Несколько серверов с контролем доступа

```ini
[SETTINGS]
language = ru
port     = 3001

[SERVER_pve]
name             = PvE сервер
database         = C:/Servers/pve/game.db
refresh_cooldown = 300

[SERVER_pvp]
name             = PvP сервер
database         = C:/Servers/pvp/game.db
refresh_cooldown = 300

[USERS]
; Формат:  логин = пароль:server1,server2
; Используйте * для доступа ко всем серверам.
superadmin = secretpass:*
pvp_admin  = pvppass:pvp
pve_admin  = pvepass:pve
; Уберите секцию [USERS] полностью, чтобы отключить авторизацию.
```

**Примечания:**
- В путях к базам используйте прямые слэши. Обратные слэши тоже допускаются.
- `refresh_cooldown` по умолчанию 300 секунд, если не указан.
- Старая секция `[CONAN_EXILES]` всё ещё поддерживается для обратной совместимости (трактуется как один сервер `server1`).

### Как работает загрузка данных

Приложение **не** читает `game.db` при открытии страницы. Вместо этого:

1. Откройте панель **Серверы** (🖥 в боковой панели).
2. Нажмите **Обновить** рядом с нужным сервером.
3. Снимок сохраняется в папку `snapshots/`, поэтому при следующих запусках данные загружаются мгновенно без обращения к базе.

## Разработка

Требования: Node.js 24+

```bash
npm install
npm start        # транспиляция + запуск через babel-node на порту 3001
npm test         # запуск тестов
```

Перед запуском положите `conan-exiles-admin-map.ini` в корень проекта (или укажите путь к реальному `game.db`).

### Сборка Windows .exe

Требуется bash / WSL:

```bash
npm run build    # на выходе build/conan-exiles-admin-map-vX.Y.Z.zip
```

После сборки отредактируйте `build/conan-exiles-admin-map.ini`, указав в `database` путь к вашему `game.db`, прежде чем запускать exe.

## История изменений

#### v0.5.0 (15 июня 2026)

- **Полный редизайн интерфейса** — новая холодная сине-серая тема (палитра moorgame.ru), шрифт Segoe UI, крупнее и читаемее текст, скруглённые поверхности и выше контраст
- **Русская локализация** — добавлена полная локаль `ru` (интерфейс + названия предметов), сделана языком по умолчанию
- **Оптимизация тайлов** — перенарезка только нативных зумов 2–4 (максимум 16×16), убраны апскейленные зумы 5–6; число тайлов сокращено с ~10 600 до ~670; ближний зум апскейлится браузером. Кэш-бастинг, чтобы обновлённые тайлы подгружались без ручной очистки кэша
- **Карта Острова Сиптах** — перенарезана из исходника; фон letterbox-рамки слит с темой вместо резкой чёрной рамки
- **Кластеризация по зуму** — метки группируются при отдалении и разбиваются на отдельные при приближении (виртуализация по вьюпорту), что убирает лаги перерисовки на больших серверах; опция «группировать на всех зумах»
- **Множественный фильтр кланов** — клик по клану включает/выключает его; пустой выбор показывает всё
- **Всегда открытая боковая панель** — «Список строений» (кланы + владельцы без гильдии) теперь панель по умолчанию и всегда видна; другие панели заменяют её по требованию
- **Метки** — яркие цвета по кланам с тёмной обводкой; отсутствующие тайлы больше не показывают иконку «битое изображение»
- **Список игроков** — сокращён до Имя / Гильдия / Последний вход; ранг и уровень перенесены в тултип игрока
- **Тултипы** — владелец кластера подписан корректно (не «Игрок»); единый формат даты `dd.MM.yyyy HH:mm`
- **Навигация по карте** — свободное перемещение (убрано принудительное центрирование); кнопки смены карты вынесены в верхний бар по центру
- Убраны логотип сайдбара и неиспользуемые веб-шрифты; крупнее и заметнее иконки сайдбара

#### v0.4.2 (июнь 2026)

- **Мультисерверность** — любое число секций `[SERVER_<id>]`; переключение через панель «Серверы»
- **Загрузка через снимки** — данные читаются из `game.db` только по кнопке «Обновить»; снимки кэшируются на диск, рестарты мгновенные
- **Контроль доступа по пользователям** — записи `[USERS]` поддерживают синтаксис `user = pass:server1,server2`; `*` — полный доступ
- **Настраиваемый кулдаун обновления** на сервер (`refresh_cooldown` в секундах)
- Панель серверов со статусом (время последнего обновления) и кнопкой обновления на каждый сервер
- Исправлена сборка exe: `better_sqlite3.node` теперь корректно упаковывается для Node 24

#### v0.4.1 (июнь 2026)

- Иконка в системном трее с действиями «Открыть в браузере» и «Остановить сервер»
- Браузер открывается автоматически при старте

#### v0.4.0 (июнь 2026)

- Добавлена поддержка карты Острова Сиптах с откалиброванными координатами
- Добавлен слой рабов
- Глобальный поиск по Ctrl+F с навигацией по результатам
- Панель кланов с числом участников, индикаторами активности и сортировкой
- Тултипы меток с бейджами тира/альфы и подсказкой телепорта
- Тёмный интерфейс — убрана зависимость от Bootstrap
- Все тайлы карты переведены из PNG в WebP (на ~30–50% меньше)
- Зумы 2–5 перегенерированы из зума 6 для точности координат
- Кэш тайлов в браузере на 365 дней
- Workflow релизов на GitHub Actions (автосборка Windows .exe)

#### v0.2.0 (ноябрь 2018)

- Добавлены фильтры строений: Алтари, Загоны, Сундуки, Картографические комнаты, Требушеты, Хранилища, Колодцы, Все ремесленные станки, Все объекты Pippi
- Убран id игрока/гильдии из легенды

#### v0.1.0 (октябрь 2018)

- Переводы на испанский (es) и английский (en)
- Поддержка Basic Auth
- Файл конфигурации для языка, порта, пути к базе и пользователей

#### v0.0.1 (октябрь 2018)

- Первый релиз
